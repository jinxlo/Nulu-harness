# Patch for resuming one seeded semantic-checkpoint session over the shipped headless profile.
- id: headless-startup
  disabled: true

- id: headless-runner
  disabled: true

- id: llm-gateway
  disabled: true

- id: session-persistence-jsonl
  config:
    root: './.sessions'
    compression: none

- insert:
    - id: replay
      name: '@worldapptechnologies/nulu-llm-replay'
      config:
        file: !!js process.env.NULU_SNAPSHOT_FILE
        overrideFile: !!js process.env.NULU_SNAPSHOT_OVERRIDE

    # Publish the persisted agent before the driver selects the single root.
    - id: resumed-agent
      name: './tests/fixtures/semantic-checkpoint-agent.ts'

- id: session-log-gateway
  config:
    enabled: false
