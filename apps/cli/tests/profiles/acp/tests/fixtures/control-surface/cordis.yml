# Keyless generic ACP v1 control-surface patch over `nulu --profile acp`.

- id: llm-deepseek
  name: '@worldapptechnologies/nulu-llm-deepseek'
  disabled: true

- id: acp
  name: '@worldapptechnologies/nulu-acp'
  config:
    provider: control-fixture
    model: alpha

- id: session-persistence-jsonl
  name: '@worldapptechnologies/nulu-session-persistence-jsonl'
  config:
    root: !!js process.env.NULU_CONFORMANCE_PERSISTENCE_ROOT
    compression: none

- insert:
    - id: control-surface-llm
      name: './control-surface-llm.ts'
