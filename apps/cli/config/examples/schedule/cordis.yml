# Opt-in Schedule patch over the shipped Web composition. The owner observes
# only roots published after this overlay loads.

- insert:
    - id: time-context
      name: '@worldapptechnologies/nulu-time-context'

    - id: schedule
      name: '@worldapptechnologies/nulu-schedule'

- id: ui-schedule
  disabled: false
