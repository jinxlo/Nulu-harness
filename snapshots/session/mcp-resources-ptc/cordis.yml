- insert:
    - id: mcp-resource-catalog
      name: '@worldapptechnologies/nulu-mcp-client'
      config:
        transport: stdio
        serverName: catalog
        command: !!js process.execPath
        args: !!js '[process.env.NULU_MCP_RESOURCES_FIXTURE]'
        failOnStartupError: true
        reconnect:
          enabled: false

- id: tools
  name: '@worldapptechnologies/nulu-tools'
  config:
    mode: ptc
