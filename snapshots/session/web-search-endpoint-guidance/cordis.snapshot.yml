# Keyless replay counterpart: the failed search remains real; only the
# conversation model adapter is replaced by replay.
- id: llm-gateway
  name: '@worldapptechnologies/nulu-llm-gateway'
  disabled: true

- id: plugin-package-inventory
  disabled: true

- insert:
    - id: llm-replay
      name: '@worldapptechnologies/nulu-llm-replay'
      config:
        providers:
          - id: worldapp-gateway
            name: Nulu
            models:
              - id: nulu-5
                contextWindow: 1000000
                defaultMaxTokens: 256000
                reasoningEfforts: ['off', 'low', 'high', 'max']
                defaultReasoningEffort: max
              - id: nulu-5-ultra

    - id: web-search-error-fixture
      name: './web-search-error-fixture.mjs'

- id: web-search-gateway
  name: '@worldapptechnologies/nulu-web-search-gateway'
  config:
    apiKey: snapshot-key
    # The fixture maps this recorded authority to its OS-assigned listener.
    baseURL: http://127.0.0.1:43118/anthropic/v1
