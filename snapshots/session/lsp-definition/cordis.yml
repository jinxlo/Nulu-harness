# Exercise the model-facing LSP tool through the shipped Loader entry path.
# The scenario workspace supplies the deterministic stdio server used by this test composition.
- insert:
    - id: lsp
      name: '@worldapptechnologies/nulu-lsp'
    - id: lsp-stdio
      name: '@worldapptechnologies/nulu-lsp-stdio'
      config:
        servers:
          fixture:
            command: !!js process.execPath
            args: ['./lsp-server.mjs']
            extensionToLanguage:
              '.ts': typescript
    - id: tool-lsp
      name: '@worldapptechnologies/nulu-tool-lsp'
      config:
        maxLocations: 1

- id: timeout-policy
  name: '@worldapptechnologies/nulu-tool-call-timeout-policy'
