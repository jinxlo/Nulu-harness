# Live-recording patch shared by the ordinary headless snapshot composition.
# The shipped profile owns application startup; this patch fixes only the
# deterministic test composition and raw persistence needed for recording.

- id: llm-gateway
  name: '@worldapptechnologies/nulu-llm-gateway'
  config:
    thinking: enabled
    reasoningEffort: max
    models:
      - id: nulu-5
      - id: nulu-5-ultra
      - id: nulu-5-vision
        inputModalities: [text, image]

- id: sandbox-policy
  name: '@worldapptechnologies/nulu-sandbox-policy'
  config:
    mode: !!js "process.env.NULU_PERMISSION_MODE ?? (process.env.NULU_SNAPSHOT === undefined ? 'workspace-write' : 'danger-full-access')"
    workspaceRoot: !!js process.cwd()

- id: approval
  name: '@worldapptechnologies/nulu-user-approval'
  config:
    policy: !!js "(process.env.NULU_PERMISSION_MODE ?? (process.env.NULU_SNAPSHOT === undefined ? 'workspace-write' : 'danger-full-access')) === 'danger-full-access' ? 'never' : 'ask'"

- id: session-persistence-jsonl
  name: '@worldapptechnologies/nulu-session-persistence-jsonl'
  config:
    root: !!js nuluHomePath('sessions')
    compression: none

- id: session-title-llm
  disabled: true

- id: system-prompt
  name: '@worldapptechnologies/nulu-system-prompt'
  config:
    personaPrefix: |
      You are a coding assistant powered by the {{model}} model. Your working directory is {{cwd}}. Your bash tool runs under a file sandbox — a `[sandbox: file access denied …]` result is policy, not a command bug.

      Verify your work by running the code or tests. Keep answers brief and factual.

- id: agent-instructions
  name: '@worldapptechnologies/nulu-agent-instructions'
  config:
    maxBytes: 65536

- id: tool-subagent
  name: '@worldapptechnologies/nulu-tool-subagent'
  config:
    provider: spawn
    toolName: subagent
    backgroundMode: continuable
    maxDepth: 1

- id: tool-subagent-fork
  name: '@worldapptechnologies/nulu-tool-subagent'
  config:
    provider: fork
    toolName: subagent_fork
    backgroundMode: one-shot
    enableRunInBackground: false
    maxDepth: 1

- id: fs-sandbox
  name: '@worldapptechnologies/nulu-fs-sandbox'
  config:
    cwd: !!js process.cwd()

- insert:
    - id: hooks-claude-code
      name: '@worldapptechnologies/nulu-hooks-claude-code'
      config:
        configPath: ./hooks.json

    - id: hooks-codex
      name: '@worldapptechnologies/nulu-hooks-codex'
      config:
        configPath: ./codex-hooks.json

- id: session-log-gateway
  config:
    enabled: false
