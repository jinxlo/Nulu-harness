# SDK-profile patch for a deterministic parent model that selects a route for
# a separate SDK child runtime. Both runtimes persist their request headers.

- id: llm-deepseek
  name: '@worldapptechnologies/nulu-llm-deepseek'
  disabled: true

- id: tool-subagent
  name: '@worldapptechnologies/nulu-tool-subagent'
  disabled: true

- id: sdk-jsonrpc-server
  name: '@worldapptechnologies/nulu-sdk-jsonrpc-server'
  disabled: true

- id: session-persistence-jsonl
  name: '@worldapptechnologies/nulu-session-persistence-jsonl'
  config:
    root: !!js nuluHomePath('sessions')
    compression: none

- insert:
    - id: mock-llm
      name: '../../../packages/subagent/subagent-nulu-sdk/tests/fixtures/loader/mock-delegating-llm.ts'
      disabled: !!js process.env.NULU_SNAPSHOT !== 'record'

    - id: subagent-nulu-sdk
      name: '@worldapptechnologies/nulu-subagent-nulu-sdk'
      config:
        profile: sdk
        patches: !!js JSON.parse(process.env.NULU_TEST_CHILD_PATCHES ?? '[]')
        nuluHome: !!js process.env.NULU_TEST_CHILD_HOME
        provider: mock
        model: mock-routed
        env:
          NULU_TELEMETRY_DISABLED: '1'

    - id: subagent-model-selection-settings
      name: '@worldapptechnologies/nulu-tool-subagent/model-selection-settings'
      config:
        enabled: true
        allowedModels:
          - provider: mock
            model: mock-routed

    - id: tool-subagent-nulu-sdk
      name: '../../../packages/subagent/subagent-nulu-sdk/tests/fixtures/loader/scoped-tool-subagent.ts'
      config:
        provider: nulu-sdk
        toolName: subagent
        enableRunInBackground: false
        agentOptions:
          maxTokens: 777
        maxDepth: 'provider-managed'

    - id: sdk-jsonrpc-server-dynamic-live
      name: '@worldapptechnologies/nulu-sdk-jsonrpc-server'
      inject: [sdkAppStartup, loader]
      disabled: !!js process.env.NULU_SNAPSHOT !== 'record'
      config:
        maxTokensAsSuccess: true
