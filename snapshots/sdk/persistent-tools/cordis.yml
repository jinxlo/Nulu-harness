# Per-launch SDK customization: retain only persistent Bash and the string
# editor as model-facing tools, with process-local providers and an explicit
# danger-full-access / never-ask policy.

- insert:
    - id: tool-str-replace-editor
      name: '@worldapptechnologies/nulu-tool-str-replace-editor'
      config:
        maxOutputChars: 16000

- id: llm-gateway
  name: '@worldapptechnologies/nulu-llm-gateway'
  config:
    apiKeyEnv: WORLD_APP_TECHNOLOGIES_API_KEY
    streamIdleTimeoutMs: 172800000
    models:
      - id: !!js process.env.NULU_MODEL ?? 'nulu-5'
        contextWindow: !!js Number(process.env.NULU_CONTEXT_WINDOW ?? 1000000)

- id: sandbox-policy
  name: '@worldapptechnologies/nulu-sandbox-policy'
  config:
    mode: danger-full-access
    workspaceRoot: !!js process.cwd()

- id: approval
  name: '@worldapptechnologies/nulu-user-approval'
  config:
    policy: never

- id: permission
  name: '@worldapptechnologies/nulu-permission-presets'
  disabled: true

- id: bash-sandbox
  name: '@worldapptechnologies/nulu-bash-sandbox'
  disabled: true

- id: pwsh-sandbox
  name: '@worldapptechnologies/nulu-pwsh-sandbox'
  disabled: true

- id: fs-sandbox
  name: '@worldapptechnologies/nulu-fs-sandbox'
  disabled: true

- id: tool-bash
  name: '@worldapptechnologies/nulu-tool-bash'
  disabled: true

- id: tool-pwsh
  name: '@worldapptechnologies/nulu-tool-pwsh'
  disabled: true

- id: agent-instructions
  name: '@worldapptechnologies/nulu-agent-instructions'
  disabled: true

- id: skill
  name: '@worldapptechnologies/nulu-skill'
  disabled: true

- id: skill-filesystem
  name: '@worldapptechnologies/nulu-skill-filesystem'
  disabled: true

- id: tool-skill
  name: '@worldapptechnologies/nulu-tool-skill'
  disabled: true

- id: tool-jobs
  name: '@worldapptechnologies/nulu-tool-jobs'
  disabled: true

- id: tool-fs
  name: '@worldapptechnologies/nulu-tool-fs'
  disabled: true

- id: tool-fs-search
  name: '@worldapptechnologies/nulu-tool-fs-search'
  disabled: true

- id: tool-subagent-control
  name: '@worldapptechnologies/nulu-tool-subagent-control'
  disabled: true

- id: tool-subagent-list-agents
  name: '@worldapptechnologies/nulu-tool-subagent-control/list-agents'
  disabled: true

- id: tool-subagent
  name: '@worldapptechnologies/nulu-tool-subagent'
  disabled: true

- id: tool-subagent-fork
  name: '@worldapptechnologies/nulu-tool-subagent'
  disabled: true

- id: tool-workflow
  name: '@worldapptechnologies/nulu-tool-workflow'
  disabled: true

- id: tool-todo
  name: '@worldapptechnologies/nulu-tool-todo'
  disabled: true

- id: tool-goal
  name: '@worldapptechnologies/nulu-tool-goal'
  disabled: true

- id: tool-ralph
  name: '@worldapptechnologies/nulu-tool-ralph'
  disabled: true

- id: tool-web
  name: '@worldapptechnologies/nulu-tool-web'
  disabled: true

- id: plan-mode
  name: '@worldapptechnologies/nulu-plan-mode'
  disabled: true

- id: system-prompt
  name: '@worldapptechnologies/nulu-system-prompt'
  config:
    includeHarnessIdentity: false
    personaPrefix: !!js process.env.NULU_SYSTEM_PROMPT ?? 'You are a helpful software engineer assistant.'

- id: session-persistence-jsonl
  name: '@worldapptechnologies/nulu-session-persistence-jsonl'
  config:
    root: !!js nuluHomePath('sessions')
    compression: none

- insert:
    - id: bash-local
      name: '@worldapptechnologies/nulu-bash-local'

    - id: fs-local
      name: '@worldapptechnologies/nulu-fs-local'
      config:
        cwd: !!js process.cwd()

    - id: terminal
      name: '@worldapptechnologies/nulu-terminal'

    - id: terminal-bash
      name: '@worldapptechnologies/nulu-terminal-bash'
      config:
        timeoutMs: 300000

    - id: persistent-bash
      name: '@worldapptechnologies/nulu-tool-bash-persistent'
      config:
        timeoutMs: 300000
        description: |-
          Run commands in a bash shell
          * When invoking this tool, the contents of the "command" parameter does NOT need to be XML-escaped.
          * You don't have access to the internet via this tool.
          * You do have access to a mirror of common linux and python packages via apt and pip.
          * State is persistent across command calls and discussions with the user.
          * To inspect a particular line range of a file, e.g. lines 10-25, try 'sed -n 10,25p /path/to/the/file'.
          * Please avoid commands that may produce a very large amount of output.
          * Please run long lived commands in the background, e.g. 'sleep 10 &' or start a server in the background.

- id: session-log-deepseek
  config:
    enabled: false
