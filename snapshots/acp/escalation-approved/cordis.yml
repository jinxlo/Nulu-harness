# ACP snapshot-record patch over the shipped `acp` profile. The nulu
# launcher owns environment loading, plugin resolution, and process shutdown;
# stdout remains reserved for ACP JSON-RPC.

- id: llm-gateway
  name: '@worldapptechnologies/nulu-llm-gateway'
  config:
    thinking: enabled
    reasoningEffort: max
    models:
      - id: nulu-5
      - id: nulu-5-ultra
      - id: nulu-5-vision
        inputModalities: [text, image]

- id: sandbox-policy
  name: '@worldapptechnologies/nulu-sandbox-policy'
  config:
    mode: !!js "process.env.NULU_PERMISSION_MODE ?? (process.env.NULU_SNAPSHOT === undefined ? 'workspace-write' : 'danger-full-access')"
    workspaceRoot: !!js process.cwd()

- id: approval
  name: '@worldapptechnologies/nulu-user-approval'
  config:
    policy: !!js "(process.env.NULU_PERMISSION_MODE ?? (process.env.NULU_SNAPSHOT === undefined ? 'workspace-write' : 'danger-full-access')) === 'danger-full-access' ? 'never' : 'ask'"

- id: session-persistence-jsonl
  name: '@worldapptechnologies/nulu-session-persistence-jsonl'
  config:
    root: !!js process.env.NULU_SNAPSHOT_SESSIONS_ROOT ?? nuluHomePath('sessions')
    compression: !!js "process.env.NULU_SNAPSHOT === undefined ? 'zstd' : 'none'"

- id: acp
  name: '@worldapptechnologies/nulu-acp'
  config:
    provider: worldapp-gateway
    model: nulu-5

- id: system-prompt
  name: '@worldapptechnologies/nulu-system-prompt'
  config:
    personaPrefix: |
      You are a coding assistant powered by the {{model}} model. Your working directory is {{cwd}}. Your bash tool runs under a file sandbox — a `[sandbox: file access denied …]` result is policy, not a command bug.

      Verify your work by running the code or tests. Keep answers brief and factual.

- id: agent-instructions
  name: '@worldapptechnologies/nulu-agent-instructions'
  config:
    maxBytes: 65536

- id: tool-subagent
  name: '@worldapptechnologies/nulu-tool-subagent'
  config:
    provider: spawn
    toolName: subagent
    backgroundMode: continuable
    maxDepth: 1

# Fork omits model selection so provider/model stay equal to the parent and the
# inherited history remains eligible for KV Cache reuse. It stays one-shot because
# a continuable child's `report` tool and prompt section precede that history and
# invalidate the same prefix. `run_in_background` is off as an explicit foreground-only
# choice even though the shipped ACP profile mounts the generic Job runtime.
# See .agents/notes/implemented/feature/2026-08-18-model-selected-subagent-routes.md
# and .agents/notes/implemented/architecture/2026-08-10-fork-children-stay-one-shot.md.
- id: tool-subagent-fork
  name: '@worldapptechnologies/nulu-tool-subagent'
  config:
    provider: fork
    toolName: subagent_fork
    backgroundMode: one-shot
    enableRunInBackground: false
    maxDepth: 1

- id: fs-sandbox
  name: '@worldapptechnologies/nulu-fs-sandbox'
  config:
    cwd: !!js process.cwd()

- insert:
    - id: hooks-claude-code
      name: '@worldapptechnologies/nulu-hooks-claude-code'
      config:
        configPath: ./hooks.json

    - id: hooks-codex
      name: '@worldapptechnologies/nulu-hooks-codex'
      config:
        configPath: ./codex-hooks.json
