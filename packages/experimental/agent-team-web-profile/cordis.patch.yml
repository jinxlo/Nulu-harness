# Experimental Agent Teams Web layer. Apply after nulu-web-app and the host-side
# nulu-agent-team-profile so the browser mounts only when the Team service is present.

- insert:
    - id: ui-agent-team
      name: '@worldapptechnologies/nulu-experimental-client-ui-agent-team'
