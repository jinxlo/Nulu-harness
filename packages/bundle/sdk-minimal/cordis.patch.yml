# Standalone minimal SDK application. Unlike the ordinary SDK profile, this
# bundle does not layer over nulu-base: this insert is the complete Cordis tree.
# User profile, home, and invocation patches still apply above it.

- insert:
    - id: sdk-app-startup
      name: '@worldapptechnologies/nulu-sdk-app'
      config:
        profile: sdk-minimal

    - id: sdk-jsonrpc-server
      name: '@worldapptechnologies/nulu-sdk-jsonrpc-server'
      inject: [sdkAppStartup, loader]
      config:
        maxTokensAsSuccess: false

    - id: llm-api-extensions
      name: '@worldapptechnologies/nulu-llm-api-extensions'

    - id: session-log-gateway
      name: '@worldapptechnologies/nulu-session-log-gateway'

    - id: plugin-package-inventory
      name: '@worldapptechnologies/nulu-plugin-package-inventory'

    - id: llm-gateway
      name: '@worldapptechnologies/nulu-llm-gateway'
      config:
        apiKeyEnv: WORLD_APP_TECHNOLOGIES_API_KEY
        defaultContextWindow: !!js Number(process.env.NULU_CONTEXT_WINDOW ?? 1000000)
        streamIdleTimeoutMs: 172800000

    - id: sandbox
      name: '@worldapptechnologies/nulu-sandbox-local'

    # Shared projection registry: sandbox-policy and terminal-bash fold
    # sandbox-mode state through its units and require it as a hard injection.
    - id: session-projection
      name: '@worldapptechnologies/nulu-session-projection'

    - id: sandbox-policy
      name: '@worldapptechnologies/nulu-sandbox-policy'
      config:
        mode: danger-full-access
        workspaceRoot: !!js process.cwd()

    - id: subprocess
      name: '@worldapptechnologies/nulu-subprocess-local'

    - id: pty
      name: '@worldapptechnologies/nulu-terminal'

    - id: terminal-bash
      name: '@worldapptechnologies/nulu-terminal-bash'
      disabled: !!js process.platform === 'win32'
      config:
        timeoutMs: 300000

    - id: terminal-pwsh
      name: '@worldapptechnologies/nulu-terminal-bash'
      disabled: !!js process.platform !== 'win32'
      config:
        shellDialect: pwsh
        timeoutMs: 300000

    # Keep the minimal agent kernel explicit: unlike nulu-base, this profile
    # owns each service row and omits every optional producer it does not use.
    - id: timer
      name: '@worldapptechnologies/cordis-plugin-timer'

    - id: llm
      name: '@worldapptechnologies/nulu-llm'

    - id: session
      name: '@worldapptechnologies/nulu-session'

    - id: session-title
      name: '@worldapptechnologies/nulu-session-title'
      config:
        fallbackMaxWords: 5
        fallbackMaxBytes: 40
        maxTitleBytes: 80

    - id: system-prompt
      name: '@worldapptechnologies/nulu-system-prompt'
      config:
        includeHarnessIdentity: false
        includeRuntimeContext: false
        personaPrefix: !!js process.env.NULU_SYSTEM_PROMPT ?? 'You are a helpful software engineer assistant.'

    - id: tools
      name: '@worldapptechnologies/nulu-tools'

    - id: mcp-resources
      name: '@worldapptechnologies/nulu-mcp-resources'

    - id: agent
      name: '@worldapptechnologies/nulu-agent'

    - id: llm-retry
      name: '@worldapptechnologies/nulu-llm-retry'

    - id: jobs
      name: '@worldapptechnologies/nulu-jobs-local'

    - id: invariants
      name: '@worldapptechnologies/nulu-invariants'

    - id: session-invariant
      name: '@worldapptechnologies/nulu-session/invariant'

    - id: agent-invariant
      name: '@worldapptechnologies/nulu-agent/invariant'

    - id: scope-invariant
      name: '@worldapptechnologies/nulu-scope/invariant'

    - id: agent-loop-invariant
      name: '@worldapptechnologies/nulu-agent-loop/invariant'

    - id: agent-loop
      name: '@worldapptechnologies/nulu-agent-loop'
      config:
        agents: []

    - id: persistent-bash
      name: '@worldapptechnologies/nulu-tool-bash-persistent'
      disabled: !!js process.platform === 'win32'
      config:
        timeoutMs: 300000
        description: |-
          Run commands in a bash shell
          * When invoking this tool, the contents of the "command" parameter does NOT need to be XML-escaped.
          * Network access depends on the task environment. Prefer configured mirrors/proxies when they are available.
          * State is persistent across command calls and discussions with the user.
          * To inspect a particular line range of a file, e.g. lines 10-25, try 'sed -n 10,25p /path/to/the/file'.
          * Please avoid commands that may produce a very large amount of output.
          * Please run long lived commands in the background, e.g. 'sleep 10 &' or start a server in the background.

    - id: persistent-pwsh
      name: '@worldapptechnologies/nulu-tool-pwsh-persistent'
      disabled: !!js process.platform !== 'win32'
      config:
        timeoutMs: 300000
        description: |-
          Run commands in a PowerShell shell
          * When invoking this tool, the contents of the "command" parameter does NOT need to be XML-escaped.
          * You don't have access to the internet via this tool.
          * State is persistent across command calls and discussions with the user.
          * Use native Windows paths (C:\...) and $env:NAME variables; this is PowerShell, not bash.
          * Please avoid commands that may produce a very large amount of output.
          * Please run long lived commands in the background, e.g. 'Start-Job' or start a server with Start-Process.

    - id: sessions
      name: '@worldapptechnologies/nulu-session-persistence-jsonl'
      config:
        root: !!js nuluHomePath('sessions')
        compression: none
