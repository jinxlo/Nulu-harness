# The SDK application over nulu-base. Stdout belongs exclusively to JSON-RPC.

- id: system-prompt
  config:
    personaSuffix: Your working directory is {{cwd}}.
    personaPrefix: >-
      You are a coding agent powered by the {{model}} model.

- id: session-title-llm
  disabled: true

- insert:
    - id: sdk-app-startup
      name: '@worldapptechnologies/nulu-sdk-app'
      config:
        profile: sdk

    - id: sdk-jsonrpc-server
      name: '@worldapptechnologies/nulu-sdk-jsonrpc-server'
      inject: [sdkAppStartup, loader]
      config:
        maxTokensAsSuccess: !!js "process.env.NULU_MAX_TOKENS_AS_SUCCESS === undefined ? true : JSON.parse(process.env.NULU_MAX_TOKENS_AS_SUCCESS)"
