/**
 * @worldapptechnologies/nulu-base — the shared nulu core as a profile bundle. The
 * package's substance is `cordis.patch.yml`, declared by the `nulu.bundle.patch`
 * manifest field and resolved by the profile composer through that field;
 * this module carries no runtime API.
 * @module @worldapptechnologies/nulu-base
 */

export {}
